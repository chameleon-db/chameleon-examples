// Todo entity
// Represents tasks owned by users

entity Todo {
    id: uuid primary,
    title: string,
    description: string nullable,
    completed: bool,
    user_id: uuid,
    created_at: timestamp default now(),
    updated_at: timestamp default now(),
    due_date: timestamp nullable,
}